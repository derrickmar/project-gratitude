<div class="modal fade" id="create-modal" tabindex="-1" role="dialog" aria-labelledby="createModal" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <h1 class="modal-title text-center" id="home-modal-title">You're Awesome</h1>
        <h3 class='text-center'>Take 30 seconds and write a short note.</h3>
        <br>
        <p class='text-center'>Your note stays anonymous. <br>So you can write whatever you want. Just write what comes to your mind first.</p>
        <div class="modal-body">
          <div class="row">
            <div class="col-sm-12">
              <%= form_for @note, url: { action: 'create' },
              :html => { :multipart => true, class: 'form-group' }, remote: true do |f| %>
              <%= f.text_area :desc, class: 'form-control create', rows: '3', placeholder: "I'm grateful for...", maxlength: '250'%><br />
              <input type="file" name="image[pic]" id="image-select" class="hidden" onchange="handleFiles(this.files)" accept="image/*"/>
              <div id="image-btn" class="btn btn-primary"> Choose image </div>
              <div class="image-preview-holder"></div>
              <%= f.submit "Create new note", class: 'btn col-xs-10 col-xs-offset-1 cta', id: 'create-note-button' %>
              <% end %>
              <!-- Do multiple for multiple files -->
              <div class="num-chars">250</div>

              <%= form_for(Image.new, html: { multipart: true, id: 'image_upload', class: "dropzone"}) do |f|  %>
              <div class="fallback">
                <%= f.file_field :image %><br>
                <%= f.submit "Upload" %>
              </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
