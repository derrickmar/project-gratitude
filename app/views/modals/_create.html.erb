<div class="modal fade" id="createModal" tabindex="-1" role="dialog" aria-labelledby="createModal" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <h1 class="modal-title text-center" id="home-modal-title">You're Awesome</h1>
        <h3 class='text-center'>Take 30 seconds and write a short note.</h3>
        <br>
        <p class='text-center'>Your note stays anonymous. <br>So you can write whatever you want. Just write what comes to your mind first.</p>
        <div class="modal-body">
          <div class="row">
            <div class="col-sm-12">
<!--               <div class="form-group">
                <textarea class="form-control create " rows="3" maxlength="250" placeholder="I'm grateful for..."></textarea>
              </div>
              <a href=""><i class="fa fa-picture-o">&nbsp;Add Photo</i></a>
              <div class="num-chars">250</div>
            </div>
            <center><button class='btn col-xs-10 col-xs-offset-1 cta create'>Create Note</button></center> -->

            <%= form_for @note, url: { action: 'create' },
            :html => { :multipart => true, class: 'form-group' } do |f| %>
            <%= f.text_area :desc, class: 'form-control create', rows: '3', placeholder: "I'm grateful for...", maxlength: '250'%><br />
            <i class="fa fa-picture-o">&nbsp;
              <%= file_field(:image, :pic) %>
            </i>
            <%= f.submit "Create new note",
            class: 'btn col-xs-10 col-xs-offset-1 cta create' %>
            <% end %>
            <div class="num-chars">250</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
