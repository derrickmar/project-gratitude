<div class="container header-container">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <div class="navbar-brand">
      <div class="content">
        <%= image_tag "pin.png", class: "pin" %>
        <%= link_to root_path, class: 'root-image' do %>
          <h1>Project Gratitude</h1>
          <h2>Because life is a <span>f*</span>cking miracle</h2>
        <% end %>
      </div>
    </div>
  </div>`
  <div id="navbar" class="navbar-collapse collapse">
    <ul class="nav navbar-nav navbar-right">
      <!-- if guest don't show sign out -->
      <li><a id="create-new-note"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span>Spread Gratitude</a> </li>
      <% if user_signed_in? && !current_or_guest_user.is_guest %>
      <li><%= link_to destroy_user_session_path, method: :delete  %></li>
      <% else %>
      <li>
        <%= link_to new_user_session_path, class: "settings" do %>
          <span class="glyphicon glyphicon-user" aria-hidden="true">
        <% end %>
      </li>
      <% end %>
    </ul>
  </div><!--/.nav-collapse -->
</div>